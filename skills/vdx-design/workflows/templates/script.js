// Page-local script. Loaded with `defer`, so the DOM is parsed by the time
// this runs. Avoid imports — bundle.js inlines this verbatim, so any
// external import would break the single-file deliverable.
